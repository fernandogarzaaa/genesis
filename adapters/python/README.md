# genesis-adapter (Python)

Zero-dependency helpers implementing the Genesis adapter protocol
(`../PROTOCOL.md`). Import from a checkout during development:

```bash
pip install -e ./adapters/python
```

or vendor the single module — it is stdlib-only by design.
