# genesis-adapter (Node)

Zero-dependency helpers implementing the Genesis adapter protocol
(`../PROTOCOL.md`). ESM, Node ≥ 20, no install step when vendored:

```js
import { runSubject, runEvaluator } from "../../adapters/node/index.js";
```
